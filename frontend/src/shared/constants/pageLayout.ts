/** Matches header inner container: logo ↔ profile button alignment */
export const PAGE_MAX_WIDTH = 1440;

export const PAGE_HORIZONTAL_PADDING = { xs: 2, sm: 3, md: 4 } as const;

/** Vertical spacing between header / main / footer */
export const MAIN_CONTENT_PADDING = {
  pt: { xs: 2, sm: 3 },
  pb: { xs: 3, sm: 4 },
} as const;
